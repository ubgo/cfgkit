# Layered on top in production. It sets only what actually differs.
LOG_LEVEL=warn
FEATURE_NEW_CHECKOUT=true

# A key no struct field binds. It is reported, never fatal — one file often
# serves several audiences, and a deploy pipeline's variables live here too.
DEPLOY_REGION=eu-west-1
